# Polymarket Arbitrage Bot - Configuration
# Remplir les valeurs ci-dessous.
# Les champs marques [REQUIRED] sont obligatoires.

# --- Identifiants Polymarket (Clob API) --- [REQUIRED]
# Obtenez vos credentials sur: https://polymarket.com/settings/api
POLY_API_KEY="your_api_key_here"
POLY_API_SECRET="your_api_secret_here"
POLY_API_PASSPHRASE="your_passphrase_here"

# Cle privee du wallet (ex: Metamask) pour la signature des transactions [REQUIRED]
# ATTENTION: Ne jamais partager ou commiter ce fichier avec votre cle privee!
PRIVATE_KEY="your_private_key_here"

# --- Parametres de Trading --- [REQUIRED]
# Capital fixe par trade (en USDC). Ex: 10.0
CAPITAL_PER_TRADE=10.0
# Marge minimale de profit (0.01 = 1%). Valeur entre 0 et 1. Ex: 0.02
MIN_PROFIT_MARGIN=0.02
# Volume minimum du marche (en $). Ex: 5000
MIN_MARKET_VOLUME=5000

# --- Gestion des Risques --- [OPTIONAL]
# Stop Loss (en % de perte max par position). Ex: 0.10 (10%)
STOP_LOSS=
# Take Profit (en % de gain cible). Ex: 0.20 (20%)
TAKE_PROFIT=
# Perte maximale journaliere autorisee (en $). Ex: 50.0
MAX_DAILY_LOSS=

# --- Parametres Avances --- [OPTIONAL]
# URL du WebSocket Polymarket (defaut: wss://ws-fidelity.polymarket.com)
CLOB_WS_URL=wss://ws-fidelity.polymarket.com
# Nombre max de tokens a monitorer (defaut: 20, soit 10 marches)
MAX_TOKENS_MONITOR=20
# Balance de fallback si l'API echoue (defaut: 1000.0)
FALLBACK_BALANCE=1000.0
